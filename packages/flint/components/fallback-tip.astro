---
import Callout from '../content-components/callout.astro';
import { getLocaleFromUrl } from '../utils';
import { useTranslation } from '../utils/dicts';
import { replacePrarms } from '../utils/translations';

type Props = {
	fallbackLocale: string;
};

const { fallbackLocale } = Astro.props;

const locale = getLocaleFromUrl(Astro.url.pathname);
const t = useTranslation(locale);
---

<Callout type="warning" title={false}>
	{replacePrarms(t.docs.fallbackTip, locale, fallbackLocale)}
</Callout>
