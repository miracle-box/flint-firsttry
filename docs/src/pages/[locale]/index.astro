---
import Hero from '@miracle-box/flint/components/landing/hero.astro';
import Layout from '@miracle-box/flint/layouts/blank.astro';
import { locales } from '@miracle-box/flint/utils';

export function getStaticPaths() {
	return locales.map((locale) => ({ params: { locale } }));
}
---

<Layout>
	<Hero />
</Layout>
