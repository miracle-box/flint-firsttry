---
import { getLocaleFromUrl, getI18nHelper } from '@i18n';
import Layout from '@layouts/landing.astro';

const locale = getLocaleFromUrl(Astro.url.pathname);
const t = getI18nHelper(locale);
---

<Layout title="Losenone">
	{t.notfound.title()}
</Layout>
